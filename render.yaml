services:
  - type: web
    name: bot-calcio-live
    env: docker
    region: frankfurt
    plan: free
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
